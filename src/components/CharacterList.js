
import React from 'react';
import Character from './Character.js';

export default function CharacterList(props){
    const {info} = props
    console.log(info)
    return (
        <div className = 'list-container'>
            {
                
                }
        </div>
    )
}
//I really hav eno idea why my code is not working properly, I am hoping eventually my frontdesk ticket will be looked at so i can switch to backend. This front end stuff has me at a loss.
